# paths (relative to the vv-data repo root)
RAW_DIR=../veda-vedanta-raw/raw_data/mvr/vishnu
DATA_DIR=.

# layout + labels (keep order consistent with raw)
BASE=/vv/data/mvr/vishnu
COLLECTION=mvr/vishnu

# parser adapter (must expose parse_file(path)->dict with keys sa, te, en)
PARSER=./tools/parsers/mvr_vishnu_adapter.py

# filenames
MANIFEST_NAME=vishnu.json
INDEX_NAME=index.json

# behavior
WRITE_HEADERS=true
GIT_AUTOCOMMIT=true
GIT_AUTOPUSH=false
